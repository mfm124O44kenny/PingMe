meta {
  name: verify if request come from user, not a non-user
  type: http
  seq: 3
}

get {
  url: http://localhost:5001/api/auth/check
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
